---
metadata:
  name: secrets-stack
  region: eu-north-1
managedNodeGroups:
  - name: workers
    privateNetworking: false
    instanceType: t3.small
    desiredCapacity: 2
    minSize: 1
    maxSize: 2
    labels:
      nodegroup: workers

